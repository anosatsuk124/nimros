{
  "name": "nimros",
  "dockerComposeFile": ["../docker-compose.yaml", "./compose.extend.yaml"],
  "service": "builder",
  "shutdownAction": "stopCompose",
  "containerUser": "root",
  "workspaceFolder": "/work",
  "workspaceMount": "source=${localWorkspaceFolder},target=/work,type=bind,consistency=cached"
}
